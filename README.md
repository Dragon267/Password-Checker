# Password-Checker
Search a directory for a specific key or password.

To setup up the project run:
    g++ -std=c++17 main.cpp -o main
    
Then to search a directory run:
    ./main <key>
Where <key> is the key you wan't to search for. Then this program will search the current directory for <key>.
